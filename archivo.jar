public static void main(String[] args) {
        
        double r;
        Scanner scanner = new Scanner(System.in);
        int x,i, n, p;
        System.out.println("Ingresar i :");
        i = scanner.nextInt();
        int [] arregloi = new int[i];    
        double [] arreglox = new double[i];    
        double [] arreglof = new double[i];    
        double [] arreglor = new double[i];    

        String[] barras1 = {" | i          | x           | f(x)         |"};
        String[] barras2 = {"         |         "};
        //String[] barras3 = Tabla(barras1,barras2);

        for (int a = 0; a < i; a++){ 
        System.out.println("Ingresar x :");        
        arregloi [a] = a;
        arreglox [a] = scanner.nextInt();
        System.out.println("Ingresar f(x) :");
        arreglof [a] = scanner.nextInt();
        
        }
     System.out.println("-----------------TABLA------------------");
     System.out.print(barras1[0]);
        for (int a = 0; a < (i); a++){
        barras2 [0] = barras2[0];
        System.out.print(barras2[0]);    
            
//String[] resultado=ConcatenarArray(array1,array2);
                   }
        System.out.print("\n");    
           
        for (int a = 0; a < i-i; a++){ 
            
      arreglor[a]=((arreglof[a]-arreglof[a+1])/(arreglox[a]-arreglox[a+1]));  
        System.out.println(arreglor[a]); 
        }
           
           
           
           
            for (int a = 0; a < i; a++){ 
     System.out.println(" |   "+arregloi[a]+"        | "+arreglox[a]+"         | "+arreglof[a]+"          | "+arreglor[a]);
                    } 
                    
         
    
     }          
    }